/*Event.observe(window, 'load', function() {
  $$('.identry').each(function(e) {
    var span = Builder.node('span');
    var input = Element.replace(e, span);
    span.appendChild(input);
    new Form.Element.Observer(input, 0.5, verifyCard.curry(span));
  });
  $('signin').observe('submit', function(event) {
    Event.stop(event);
    $('signin').request({onComplete: handleSigninComplete});
  });
});*/
